{
  "name": "phoneup-round-robin",
  "private": true,
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm --parallel -r dev",
    "typecheck": "pnpm -r typecheck"
  }
}
